typedef struct Deck Deck; // A stack... without calling it stack (LIFO list)
typedef struct List List;
typedef struct Pool Pool;
typedef struct Queue Queue; // FIFO list
typedef struct Table Table; // HashMap
// TODO: Some sort of Priority or Order list (priority queue)

